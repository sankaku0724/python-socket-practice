「username@abceefg」のように入力すると、全クライアントではなく、usernameのクライアントのみに送信し、そのクライアントのみで「usernameからのダイレクトメッセージ>abceefg」と表示するといったようにした.

また、ダイレクト通信をするユーザがいなかった場合は「そのユーザは存在しません」とクライアントに返すようにした。

また、ユーザ名が重複している場合は入力をやり直させるようにした。